# 2026-03-15 记忆日志

## 📌 重要事件

### 1. 钉钉通道会话路由修复 (11:35-11:40)

**问题：** 钉钉消息会话路由不稳定，WebUI 测试后消息被路由到 heartbeat group

**根本原因：** 钉钉连接器 v0.7.8 的设计问题
- 插件使用 `sessionKey: sessionContextJson` 调用 Gateway
- Gateway 根据 `X-OpenClaw-Memory-User` header 创建 `openai-user` 会话
- 这是插件内部路由逻辑，配置无法完全解决

**解决方案：**
1. 配置 `separateSessionByConversation: false` - 按用户维度路由
2. 移除 `sessionTimeout` - 禁用会话超时
3. 移除 `asyncMode` - 使用 AI Card 模式
4. 接受 `openai-user` 会话作为钉钉工作会话
5. 清理空壳 `dingtalk-connector:direct` 会话

**最终配置：**
```json
{
  "channels": {
    "dingtalk-connector": {
      "enabled": true,
      "separateSessionByConversation": false,
      "dmPolicy": "open",
      "allowFrom": ["*"]
    }
  }
}
```

**会话状态（2 个）：**
1. `agent:main:main` - WebChat 主会话（heartbeat、WebUI）
2. `agent:main:openai-user:{...}` - 钉钉工作会话（实际使用）

**功能确认：**
- ✅ 钉钉消息正常接收和回复
- ✅ 会话不会超时重开
- ✅ WebUI 测试不影响钉钉路由

---

### 2. GitHub 自动推送钉钉通知 (18:07-18:21)

**需求：** 自动任务的结果都要发到钉钉

**实现：**
1. 创建 `scripts/send-dingtalk-notify.js` - 独立钉钉通知脚本
   - 使用钉钉 API `/v1.0/robot/oToMessages/batchSend`
   - 支持文本消息发送
   - userIds 必须是数组格式

2. 修改 `scripts/auto-github-push.sh`
   - 推送完成后调用 `send-dingtalk-notify.js`
   - 成功/失败都会发送钉钉通知

3. 更新 `HEARTBEAT.md`
   - 移除 pending-notify.txt 处理逻辑
   - 标注 GitHub 推送直接发送钉钉通知

**测试成功：**
- 测试提交：`7431cec`
- 钉钉收到通知："✅ GitHub 推送成功"
- processQueryKey: `zsQ6EJfSxvg3fWe7+BhAqbw1FxSdrDMIbqleSW7PKz4=`

---

### 3. Memos Cloud 记忆能力恢复 (18:45-18:57)

**状态检查：**
- MemOS Cloud API Key 已配置
- 本地记忆文件：`2026-03-02.md`, `2026-03-10.md`
- 索引文件缺失（dirty 状态）

**操作：**
1. 重建本地记忆索引
   - 创建 `memory/chunks.json` (2 个片段)
   - 创建 `memory/meta.json` (元数据)

2. 测试 MemOS Cloud API
   - 控制台显示 SUCCESS
   - 之前 curl 测试失败是 endpoint 问题

3. 重新启用云端记忆配置
   - `recallEnabled: true`
   - `addEnabled: true`
   - 重启网关

**当前状态：**
- ✅ 本地记忆层：Nomic Embedding，支持离线语义搜索
- ✅ 云端记忆层：MemOS Cloud，正常连接
- ✅ 混合记忆模式：务实策略，能用就用

---

## 📊 系统状态

| 组件 | 状态 | 说明 |
|------|------|------|
| **系统版本** | 2026.3.13 | 最新版 |
| **网关 PID** | 41776 | 运行中 |
| **会话数量** | 2 个 | WebChat + 钉钉 |
| **GitHub 推送** | 每 6 小时 | 钉钉通知 |
| **记忆系统** | 混合模式 | 本地 + 云端 |

---

## 🎯 待办事项

- [ ] 测试 Memos Cloud 记忆搜索功能
- [ ] 监控 GitHub 推送钉钉通知稳定性
- [ ] 定期清理过时会话文件
